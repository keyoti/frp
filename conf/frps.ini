[common]
bind_port = 7001
vhost_https_port = 443

authentication_timeout = 0
conveyor_auth = http://localhost:5000/api/jwt
conveyor_subdomain_not_found_redir = https://conveyor.cloud/Home/SubdomainNotFound
conveyor_load_registry = http://localhost:5001/api/FRPSRegistry/
oldest_supported_client_version = 4
log_file = ./frps.log
log_level = info
log_max_days = 7

subdomain_host = testhost.com

[common]
dashboard_port = 7501
dashboard_user = jim
dashboard_pwd = bollocksQQ#$


# pool_count in each proxy will change to max_pool_count if they exceed the maximum value
max_pool_count = 5

# max ports can be used for each client, default value is 0 means no limit
max_ports_per_client = 10